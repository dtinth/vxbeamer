---
"website": patch
---

Give the message drag a custom image. Before, dragging a message bubble out to another app used the browser's default screenshot. This default image showed the bubble's round corners as solid squares. It also froze the color-sweep effect partway through. Now, the drag shows a clean rectangle copy of the bubble, without the color-sweep effect. Tested on iPadOS Safari and Android Chrome.
