---
"vxasr": patch
---

Remove a `latencyMs` field nothing produced, share `quoteId` between the provider registry and the configuration catalogue, and derive the frontend's audio constants from `vxasr/audio` rather than restating them.
