---
"vxasr": minor
---

Add `microsoft/mai-transcribe-2` as a second OpenRouter configuration, next to `mai-transcribe-1.5`. It costs about a third as much, but made two small transcription errors on the test fixture that `1.5` did not, so `1.5` stays the default and this is offered as an extra choice.
