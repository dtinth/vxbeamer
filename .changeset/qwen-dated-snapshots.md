---
"vxasr": minor
---

Add pinned Qwen3-ASR-Flash snapshots (`2026-02-10`, `2025-10-27`) as selectable configurations. The undated `qwen3-asr-flash-realtime` id floats — DashScope repoints it at a new snapshot without notice — so listing the pinned ids lets that drift be seen rather than inferred. Same protocol and same `DASHSCOPE_API_KEY`; no new credentials.
