{
  "name": "desktop",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mise exec -- tauri dev",
    "build": "mise exec -- tauri build",
    "check": "mise exec -- cargo check --manifest-path src-tauri/Cargo.toml",
    "test": "mise exec -- cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.10.1"
  }
}
