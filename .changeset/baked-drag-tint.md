---
"website": patch
---

Bake a flat charge tint into the message drag image: the dragged bubble now carries a solid wash of the charge sweep's lightest tint (rather than being plain), so it reads as gently charged. Flat, not the gradient — a still image can't sweep.
